# 《雾州侠行》增量执行案：工程开工闸门

> 执行状态：Phase 1～3 已完成；Phase 4 的 Lua 5.1 离线门禁已完成，Y3 定制 Lua 5.4 与真实平台/轻功灰盒仍待引擎侧验证。

## Phase 1：环境与版本

- 关闭 Y3 编辑器自动保存。
- 建立首个 Git 提交与 `baseline-y3-2.0-v17` 标签。
- `.y3maker` 作为外部工具链锁版本；`y3-lualib` 作为子模块固定提交。

## Phase 2：契约冻结

- 采纳 ADR-0005，修正文档中的事件、收据、manifest 与槽 1 结构冲突。
- 公共代码仅使用 Lua 5.1/5.4 交集语法。

## Phase 3：Lua 基础包

- 创建 `maps/EntryMap/script/wzx` 的 domain/application/adapters/config/bootstrap/tests。
- 实现 Result、ID、事件信封、规范编码、SHA-256、收据、确定性 RNG、SaveEnvelope。
- 实现六个平台服务端口、Fake/Unavailable 实现、注册表与容器。
- 将示例入口收敛到可热重载 bootstrap。

## Phase 4：质量门禁

- 建立 PowerShell 测试命令和 Lua runner。
- 执行语法、依赖边界、黄金向量、注册表、端口与 bootstrap 测试。
- 运行 Lua 审查；Y3 运行时可连接时执行同一基础冒烟测试。

## 已形成的并行开发边界

- 共享区 `domain/common`、`application/ports`、`bootstrap`、聚合注册表与入口继续由集成负责人单写。
- 玩法系统在自己的领域、应用、配置声明与测试目录内开发，通过 registrar/schema/SaveCodec/ScreenDescriptor 接入；不得直接改共享注册表或保存整槽。
- 主地图、主 SceneUI、物编与生成产物保持单写；并行验证使用独立测试地图、Prefab 或 fixture。
- 所有真实平台能力保持 `UNVERIFIED` 和关闭状态，直到对应服务验证报告具有可复核证据。
