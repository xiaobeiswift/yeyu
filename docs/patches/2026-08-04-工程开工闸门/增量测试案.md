# 《雾州侠行》增量测试案：工程开工闸门

| ID | 用例 | 预期 |
|---|---|---|
| PT-01 | Lua 5.1 加载全部 domain/application 模块 | 无语法或依赖错误 |
| PT-02 | SHA-256 标准向量 | 与标准摘要完全一致 |
| PT-03 | CanonicalReceiptHashV1 黄金向量 | 字节与 receipt ID 固定 |
| PT-04 | 规范编码非法类型/浮点/nil | 明确拒绝 |
| PT-05 | DomainEvent 与 CombatEvent | 必填字段、ID、revision/sequence 规则生效 |
| PT-06 | EventSchemaRegistry | 重复事件与生产者冲突被拒绝 |
| PT-07 | SectionOwnerRegistry | 重复 owner、路径重叠和越权写入被拒绝 |
| PT-08 | 六个端口 Fake | 方法齐全、Result 回调最多一次 |
| PT-09 | SaveEnvelope | 三层限制、版本、checksum 字段校验生效 |
| PT-10 | Fake bootstrap | 容器和功能开关可构造，无 `y3.*` 依赖 |
| PT-11 | 静态边界 | domain/application 不引用 `y3.*` |
| PT-12 | Y3 热重载入口 | 启动日志无阻断错误；不可连接时记录未验证而非伪造通过 |
| PT-13 | 22 个端口操作 DTO | 请求/结果闭合集、回显、排序、revision、概率版本与槽号约束生效 |
| PT-14 | 两阶段端口调用 | 同步拒绝不回调；精确 admission 后 completion 非内联且最多一次 |
| PT-15 | 幂等写完成态 | 成功与错误绑定原 request_key；错绑或不可判定结果转 RESULT_UNKNOWN |
| PT-16 | 端口负载防护 | cycle、alias、稀疏/混合表、非有限数、深度/节点/字符串预算均 fail-closed |
| PT-17 | 只读 authority | Port spec、App/service facade、registries 与 runtime host 不可 shadow/替换 |
| PT-18 | RuntimeHost/ReloadGuard | 伪造全局、失败重载、直接 stop/restart 不破坏唯一活动 authority |

## 本轮结果

- 命令：`tools/test.ps1 -Repeat 2 -FailFast`
- Lua 5.1 语法检查：72 个文件通过。
- domain/application 纯 Lua 静态边界扫描：通过。
- 离线测试：101 项/轮，连续 2 轮共 202 passed、0 failed。
- Y3 定制 Lua 5.4、真实账号平台服务和轻功地图灰盒本轮未执行，继续保持 `UNVERIFIED`，不得由离线结果代替。
