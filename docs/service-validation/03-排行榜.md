# 排行榜验证记录

**当前状态：UNVERIFIED。** `arena=false`，不发布排名值。

## 前置与步骤

- 至少三个测试账号、排行榜槽 101 和可查看排名原始值的权限。
1. 写入最小、正常、赛季编码边界和平台整数上界，回读自身排名与原值。
2. 查询附近排名，确认能获得构建对手所需的稳定身份或可关联引用。
3. 测试同值排序、更新延迟、重复发布、降分/升分、跨赛季编码和旧赛季排序。
4. 覆盖身份缺失、条目消失、越界值、服务超时和结果陈旧。

## 通过与降级

- 排名、原值、身份关联和整数边界都稳定才可标记 `AVAILABLE`。
- 只能展示自身值则标记 `LIMITED`，不得据此开放正式异步挑战。
- 无身份关联或赛季编码不可靠时标记 `UNAVAILABLE`，仅显示本地成绩。
