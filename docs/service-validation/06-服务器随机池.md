# 服务器随机池验证记录

**当前状态：UNVERIFIED。** `paid_gacha=false`，不得接真实抽取或真实商品。

## 前置与步骤

- 沙盒随机池、测试凭证、权益整数槽、原始请求查询和至少 90 天审计导出能力。
1. 验证结果只由服务器产生，客户端不能指定奖励或本地重抽。
2. 对同一 `request_key/context.idempotency_key` 重放、超时后查询、迟到、重复、乱序和未知池配置；每次重试或查询使用新的 `context.request_id`。
3. 分别在扣凭证、产结果、写权益三个边界注入中断；确认能查询同一原结果并幂等对账。
4. 验证保底相关单调 totals 的 CAS、跨同类型池继承、重复权益转换和迁移。
5. 导出匿名抽取记录，核对奖励表、概率、保底解释、时间范围和不可篡改审计链。

## 通过与降级

- 原子事务或等价可恢复协议、结果查询、权益对账和审计全部通过，且合规复核完成，才可考虑 `AVAILABLE`。
- 任一项受限或证据不足都保持 `UNVERIFIED/LIMITED`，真实付费永久关闭；只允许与平台权益隔离的测试币卡池。
